- defaults:
    link:
        relink: true

- clean: ['~']

- link:
    ~/.vimrc: 
      force: true
      path: vimrc
    ~/.bashrc: 
      force: true
      path: bashrc
    ~/.gitconfig:
      force: false
      path: gitconfig
- shell:
    - [git submodule update --init --recursive, Installings submodules]
